# Third-Party Assets

Use this folder for externally licensed asset packs. Keep each vendor separate so provenance stays clear.

Recommended structure:

```text
VendorName/
  licenses/
  PackName/
    raw/
    processed/
    README.md
```

Keep original downloaded files in `raw/`. Put cropped, renamed, converted, or atlas-ready versions in `processed/`.
