# Atomic Realm Assets

Place Atomic Realm environment packs here.

Recommended structure for each pack:

```text
PackName/
  raw/
  processed/
  README.md
```

Keep license files in `licenses/`. Do not edit files in `raw/` directly; put project-specific edits or exports in `processed/`.
